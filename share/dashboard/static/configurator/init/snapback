# Generated by Signal18 replication-manager v3.0.27 on 2025-04-23T09:17:49Z 
#!/bin/bash
FILE=/data/replication_manager_schema/snapback.frm    
if [ -f $FILE ]; then
   echo "Need snapback $FILE exists."
   LASTSNAP=`zfs list -r -t all |grep zp.vol | grep daily | sort -r | head -n 1  | cut -d" " -f1`
    stop
   zfs rollback $LASTSNAP
    start

else
   echo "No need snapback File $FILE does not exist."
fi