{
  "mcpServers": {
    "replication-manager": {
      "type": "sse",
      "url": "https://localhost:10005/api/mcp/sse",
      "headers": {
        "Authorization": "Bearer ${REPLICATION_MANAGER_API_TOKEN}"
      }
    }
  }
}
